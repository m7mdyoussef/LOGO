
import Foundation

enum Constants {
    enum DBName {
        static let postENt: String = "PostENt"
    }
    enum Title {
        static let mainTitle: String = "LOGO"
    }
    enum PlaceHolder {
        static let searchPosts: String = "Search Posts"
    }
}
